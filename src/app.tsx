import * as React from 'react';
import './app.css';

export default class App extends React.Component {
    render() {
        const element =
            <div>App</div>;
        return element;
    }
}